{
  "name": "mock",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "bun run --watch index.ts | pino-pretty",
    "start": "bun run index.ts"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "pino-pretty": "^13.0.0"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@navikt/fhir-mock-server": "^0.0.6"
  }
}
