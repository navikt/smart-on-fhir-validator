name: CI

on:
  pull_request:
  push:
    branches: [main]
  workflow_call:

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  verify:
    name: Lint, typecheck and test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v6
        with:
          node-version-file: .nvmrc
          cache: yarn
      - run: corepack enable
      - run: yarn install --immutable
      - run: yarn fmt:check
      - run: yarn lint
      - run: yarn tsc
      - run: yarn workspace e2e run tsc
      - run: yarn test:coverage
      - run: yarn build

  e2e:
    name: End-to-end
    # Gated behind verify so a PR that fails lint/typecheck/tests never pays for a Chromium
    # download and browser-dependency install — the slowest part of this pipeline.
    needs: [verify]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v6
        with:
          node-version-file: .nvmrc
          cache: yarn
      - run: corepack enable
      - run: yarn install --immutable
      - uses: actions/cache@v4
        with:
          path: ~/.cache/ms-playwright
          key: playwright-chromium-${{ runner.os }}-${{ hashFiles('yarn.lock') }}
      - run: yarn e2e:install
      # The landing page is statically prerendered, so the "try it against the mock EHR" link is
      # compiled out unless the flag is set at *build* time — setting it only on the server would
      # be too late for the e2e suite to find the link.
      - run: yarn build
        env:
          ENABLE_MOCK_EHR: 'true'
      - run: yarn e2e
      - uses: actions/upload-artifact@v4
        if: failure()
        with:
          name: playwright-report
          path: e2e/playwright-report/
          retention-days: 7
